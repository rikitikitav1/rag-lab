# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# venv (commit uv.lock, not .venv)
.venv/

# env / secrets
.env

# temp files (project rule)
temp_files/

# logs
*.log

# tests
.pytest_cache/
.coverage
htmlcov/

# IDE / OS
.idea/
.vscode/
.DS_Store

# internal / not for public repo
PROGRESS.md
resume
creds-shared.db
.claude/
/log

# generated / third-party-derived eval data (regenerate via evals/build_questions.py)
answers_interview.jsonl
questions_interview.md

# taxonomy scratch dumps (dev artifacts, not used at runtime)
categories_1
categories_1_sdp
categories_raw_sdp
datasets/measurements/grid/
# a measurement and the pool it read are artifacts of a run: the number and its table live in
# the journal entry, the files live beside the stand and, when they are worth sharing, on the hub
datasets/candidates/
datasets/measurements/
