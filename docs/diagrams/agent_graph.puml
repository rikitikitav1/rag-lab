@startuml agent_graph
' generated by scripts/graph_to_puml.py from the compiled graph, do not edit by hand
hide empty description

state model
model : call the LLM with the tools this hop is allowed
state retrieve
retrieve : our dispatch, raw results into the state,
retrieve : no message for the model
state grade
grade : the grader reads each chunk and drops
grade : what it calls foreign; off, a pass-through
state fallback
fallback : the coverage verdict fired: drop weak,
fallback : announce, open external
state emit
emit : tool messages for the model, after the verdict
state final
final : last turn without tools,
final : agent.no_evidence when nothing answered

[*] --> model
emit -[dashed]-> final
emit -[dashed]-> model
fallback --> emit
grade -[dashed]-> emit
grade -[dashed]-> fallback
model -[dashed]-> final
model -[dashed]-> model
model -[dashed]-> retrieve
retrieve --> grade
final --> [*]
@enduml
